{
  "name": "nexguard-whatsapp-bot-manager",
  "version": "1.0.0",
  "description": "A desktop application to manage the NexGuard WhatsApp bot",
  "main": "index.js",
  "scripts": {
    "start": "electron .",
    "install-deps": "npm install --production",
    "package": "electron-packager . NexGuardBotManager --platform=win32,linux --arch=x64 --prune=true --out=release-builds"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.2",
    "dotenv": "^16.3.1",
    "electron": "^28.1.0",
    "node-cron": "^3.0.3",
    "qrcode-terminal": "^0.12.0",
    "whatsapp-web.js": "^1.27.0"
  },
  "devDependencies": {
    "electron-packager": "^17.1.2"
  }
}
